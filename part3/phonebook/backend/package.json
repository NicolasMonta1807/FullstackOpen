{
  "name": "phonebook",
  "version": "0.1.2",
  "description": "Web-app for managing contacts",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "cd ../phonebook && npm run build && cp -r build ../phonebook-backend/",
    "deploy": "fly deploy",
    "logs:prod": "fly logs"
  },
  "author": "Nicolas Montañez",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^6.8.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
